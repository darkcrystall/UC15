import { Button, StyleSheet, Text, View } from "react-native";
import ListPokemon from "../components/ListPokemon";

const Busca = ({ navigation }: any) => {
  return (
    <View>
      <ListPokemon></ListPokemon>
    </View>
  );
};

export default Busca;

const styles = StyleSheet.create({});